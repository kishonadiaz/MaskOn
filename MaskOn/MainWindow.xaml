<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MaskOn"
        xmlns:Wpf="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf" x:Class="MaskOn.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" Topmost="True" Foreground="{x:Null}"  MouseDown="MouseDownWindow" ResizeMode="CanResize"  Background="{x:Null}" WindowStartupLocation="CenterScreen" MouseDoubleClick="WindowDoubleClicked" MouseEnter="MouseIn" d:DesignHeight="1707.954" d:DesignWidth="1996.014" WindowState="Maximized" WindowStyle="None" AllowsTransparency="True">


    <Grid >

        <StackPanel Panel.ZIndex="99" Height="1000"  IsHitTestVisible="True" VerticalAlignment="Top">
            <Border Panel.ZIndex="100" HorizontalAlignment="Center"  CornerRadius="12" x:Name="Border" MouseLeftButtonDown="DragBar_MouseLeftButtonDown" Width="122" Height="60" Background="Blue" VerticalAlignment="Top" MouseLeftButtonUp="Drag_ReleaseLeftClick" MouseLeave="MouseLeaving">
                <Grid>

                    <!-- DRAG BAR -->
                    <Grid Height="30" VerticalAlignment="Center"
                  Background="Transparent" Panel.ZIndex="1000"
                 >
                    </Grid>



                </Grid>

            </Border>
           


        </StackPanel>


        <!--
-->
        <Grid Background="Black" x:Name="Moverg" >
            
        </Grid>
        <Wpf:WebView2 IsHitTestVisible="True" Focusable="True"  Panel.ZIndex="-1"   x:Name="webview"  PreviewMouseLeftButtonDown="WebviewLeftButtonDown"   RenderTransformOrigin="0.5,0.5"  >
            <Wpf:WebView2.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform Y="50"/>
                </TransformGroup>
            </Wpf:WebView2.RenderTransform>
        </Wpf:WebView2>
    </Grid>
</Window>
